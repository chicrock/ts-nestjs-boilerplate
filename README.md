# NestJS + Typescript Boilerplate

## Install

```
# Install nestjs cli
npm i -g @nestjs/cli

# Create new project
git clone https://github.com/nestjs/typescript-starter.git [project]
cd [project]
npm install
npm run start
```
